query GetCommentsByThreadId($threadId: String!) {
  getCommentsByThreadId(threadId: $threadId) {
    data {
      id
      updated
      content
      createdBy {
        id
        username
      }
      createdAt
    }
    error
  }
}
